<launch>

    <arg name="config_file" default="mesh_visualiser_config.yaml"/>

    <node pkg="opengl" type="opengl_node" name="opengl" output="screen">
    <rosparam command="load" file="$(find mesh_visualiser)/config/$(arg config_file)"/>

    <!-- set a private parameter for the node -->
    
    </node> 
    
</launch>